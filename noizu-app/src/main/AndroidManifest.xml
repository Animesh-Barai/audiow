<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ismaeldivita.noizu.podcast">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ismaeldivita.noizu.podcast.ismaeldivita.noizu.application.PodkastApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="ismaeldivita.noizu.podcast.ismaeldivita.noizu.MainActivity" />

        <activity android:name="ismaeldivita.noizu.podcast.ismaeldivita.noizu.ui.screen.setup.SetupActivity" />

        <activity
            android:name="ismaeldivita.noizu.podcast.ismaeldivita.noizu.ui.screen.launch.LaunchActivity"
            android:noHistory="true"
            android:stateNotNeeded="true"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
