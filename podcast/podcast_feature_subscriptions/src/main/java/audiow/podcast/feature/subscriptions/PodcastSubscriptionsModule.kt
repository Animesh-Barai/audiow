package audiow.podcast.feature.subscriptions

import dagger.Module
import audiow.podcast.feature.subscriptions.ui.BindingModule

@Module(includes = [BindingModule::class])
abstract class PodcastSubscriptionsModule